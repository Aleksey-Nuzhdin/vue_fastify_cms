# Фаза 5 — синтез: дорожная карта улучшений VFC (2026-07-12)

Вход: `update_1_map.md` (у1), `update_2_architecture.md` (у2),
`update_3_security.md` (у3), `update_4_consistency.md` (у4),
`update_5_quality.md` (у5), `update_backlog.md` (bl) + решения по 4 развилкам
(у4, подтверждены владельцем 2026-07-10).

Правило работы (из `update_plan.md`): **один пункт → отдельная задача →
отдельный коммит.** Ничего не чинить пачками.

Шкалы: эффект — крит / высокий / средний / низкий;
стоимость — мин / низкая / средняя / высокая.

---

## Тезисы

- **Порядок этапов не случаен**: сначала среда и ворота (без них правки нельзя
  проверить), потом безопасность, потом мелкие реальные баги, потом крупные
  унификации, миграции данных — в конце, DX — фоном.
- **Самый выгодный пункт всей карты — SEC-2** (единая проекция user без
  `password`): один фикс закрывает сразу пять находок из четырёх отчётов
  (у3:H3, у3:L2, у2:п.2, у4:срез 1, у5:§3.1) — и утечку хешей, и «врущие»
  типы клиента.
- **zod не удалять** при чистке зависимостей: он мёртв сейчас (у5:§8), но
  именно на нём построен план валидации входа (VAL-1 ← у3:M1).
- ~~Одна развилка осталась открытой: SEC-4~~ — **решено владельцем
  2026-07-12: вариант A** (guard читает Redis-ключ access-токена — мгновенный
  отзыв при logout/logoutAll/смене пароля).
- Миграции данных (`vereficator`, `initionalValues`) — решения уже приняты
  (у4), но выполнять их стоит после стабилизации кода: каждое — сквозное
  переименование + миграция документов, откатывать больно.

---

## Этап 0 — разблокировка среды и ворота качества (GT)

Без GT-1/GT-2 остальные этапы нельзя безопасно делать: клиентские правки
нечем проверить, серверные — никто не типочекает.

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| GT-1 | ✅ `pnpm install` в `client/` — оживить node_modules (симлинки указывают на переименованную `vue_fastify_cms1`); проверить, что `lint` и `type-check` проходят | высокий | мин | сделано 2026-07-14 (владельцем) — `lint`/`type-check` на клиенте проходят | у5:§2 |
| GT-2 | ✅ Серверу — ворота типов: `"typecheck": "tsc --noEmit"` в scripts. Сейчас проверка чистая (0 ошибок) | высокий | мин | сделано 2026-07-14 — скрипт добавлен, **не подключён в `build`** (сознательное решение при правке package.json — вызывать отдельно/через CI) | у5:§1 |
| GT-3 | ✅ Удалить `client/package-lock.json` (npm-двойник рядом с pnpm-lock) | низкий | мин | сделано 2026-07-14 | у5:§4.4 |
| GT-4 | ~~Удалить бесхозный `shared/dist`~~ — уже отсутствует в репозитории, пункт неактуален | низкий | мин | закрыто без действий (проверено 2026-07-14) | у1:§5, у5:§1 |

## Этап 1 — безопасность: не выкатываться без этого (SEC)

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| SEC-1 | ✅ Стойкие секреты: случайные `JWT_SECRET`/`COOKIE_SECRET` на окружение; в `initEnv()` — проверка стойкости, fail-fast; в `.env.example` — плейсхолдер | крит | низкая | **сделано 2026-07-14**: `isWeakSecret()` = hex-32 (`^[0-9a-f]{64}$`) + энтропия ≥10, fail-fast в prod (`configs/index.ts:26-51`); `REQUIRED_SECRETS` расширен до всех 13 обязательных env (presence-check), `REQUIRED_WEAK_SECRETS` — только 2 секрета для стойкости; `.env.example` → плейсхолдеры `*_HEX_32`. Закрывает у3:C1 + у3:L1. Секреты генерятся скриптом `scripts/gen-secrets.mjs` (CSPRNG, идемпотентный, `--force` для ротации; правило стойкости дублирует серверное — держать в синхроне). NB: расширенный presence-check требует **все 13 переменных** в `.env`/`.env.production` — включая `MAILER_*` (частично закрывает старт-проверку из SEC-5) | у3:C1 |
| SEC-2 | ✅ Единая проекция user через whitelist-маппер `toPublicUser` (контракт `ReturnUser`) в `users.service` + `profile.updateProfile` через тот же маппер | высокий | низкая-средняя | **сделано 2026-07-15**: срез вынесен **на уровень сервиса, а не репозитория** (репозиторий сознательно отдаёт полный документ — санитайз это ответственность границы наружу; так blast radius не задел `auth`). `toPublicUser(u: ReturnUser<ObjectId \| string>): ReturnUser` — один маппер (`users.service`), применён в getAll/getById/create/update/findList и импортом в `profile.updateProfile`. `auth.service`/`users.repository` **не тронуты** → `findByEmail`+`findById` по-прежнему с `password` → логин и `changePassword` целы. `buildUpdate` остался только на входе (сборка `$set`). `ReturnUserList<T>` в shared переведён на элемент-дженерик; клиент `admin.users.api` → `ReturnUserList<ReturnUser>` (типы клиента перестали врать). Оба typecheck зелёные (сервер `tsc --noEmit` + клиент `vue-tsc --build`). Закрывает у3:H3+L2, у2:п.2, у4:срез 1, у5:§3.1 | у3:H3 |
| SEC-3 | ✅ `/reports/list`: не-админу **всегда** подставлять `userId = свой` (игнорируя присланный); потолок `limit` (например ≤100) | высокий | низкая | **сделано 2026-07-21**: в `findList` (`reports.service.ts`) условие форса свёл к `if(!['admin','vereficator'].includes(performedBy.role)) findQuery.userId = performedBy._id` — обычному юзеру свой `userId` подставляется **всегда**, игнорируя присланный. Закрыт обход через `?userId=<чужой>` (срабатывал, пока стояло `userId === undefined &&` — force-self включался только когда id не прислали). Верификатор наравне с админом видит чужие доклады в `/list` (согласовано с его привилегией в `update`/`delete`, `chekRoles=['admin','vereficator']`). Потолок limit: `Number(limit) > 0 ? Math.min(Number(limit), 100) : 10` — покрывает NaN/пусто/0/отрицательное→10, огромное→100; `skip` тоже прогнан через `Number()`. `count` не задет: в `$facet` (`reports.repository.ts`) `$count` считается независимо от `$skip/$limit` → total = все совпадения. typecheck зелёный. **Смежные находки (НЕ в scope SEC-3)**: `findById`/`GET /reports/item/:id` — без проверки владельца, любой авторизованный читает любой доклад по id (тот же класс IDOR); `$regex` из `title`/`authorName` не экранирован → VAL-1. Закрывает у3:H2 | у3:H2 |
| SEC-4 | ✅ Отзыв access-токена, **вариант A (утверждён 2026-07-12)**: guard после проверки подписи читает уже пишущийся ключ `user:{id}:token:access:{uuid}` — нет ключа → 401; мгновенный отзыв, цена — +1 Redis-запрос на авторизованный запрос | высокий | низкая | **сделано 2026-07-21**: в фабрике `authRoleGuard` (`authRole.guard.ts`) — одна точка для всех guard'ов (requireAuth/Admin/Manager/Vereficator). После `jwtVerify()`, проверки `type==='access'` и полей (в проверку добавлен `!user.uuid`) и **до** проверки роли: `if(!(await request.server.redis.exists(key))) throw unauthorizedError('Invalid token')`, где `key='user:${_id}:token:access:${uuid}'`. **`exists`, не `get`** — значение access-ключа пустая строка `''` (`auth.service.ts:136`); `get('')`→falsy отверг бы валидный токен (ключевая мина варианта A). Проверка вынесена **за `try/catch`** (в `try` остался только `jwtVerify`): отзыв уходит своим 401, а падение Redis прокидывается как 500, не маскируясь под ложный 401 всем авторизованным. Порядок `jwtVerify`→Redis обязателен и по данным (`key` строится из уже проверенного payload), и по смыслу (мусор режется локально до похода в Redis). Отзыв и так уже происходил (`logout`/`logoutAll`, а `changePassword`/`resetPasswordWithCode` дёргают `logoutAll` — чистят ключ) — guard просто перестал его игнорировать. Выпуск токенов и клиент не тронуты. typecheck зелёный. Закрывает у3:H1 | у3:H1 |
| SEC-5 | ✅ Mailer: добавить `MAILER_IS_ACTIVE` в `.env`/`.env.production` (+MAILER_* в prod); в заглушке не печатать код сброса в stdout; проверку конфигурации перенести на старт | высокий (prod) | низкая | **сделано 2026-07-21**: 1) заглушка `mailer.ts` больше не течёт кодом сброса — дамп `dataTransporter` (содержал `text`/`template` с 6-значным кодом) убран под `NODE_ENV==='development'`, в prod не логируется ничего. Выключатель `MAILER_IS_ACTIVE!=='true'` при этом гейтит **весь** бранч заглушки (`return 'stubbed'`) во всех env, а не только dev — иначе prod-inactive проваливался бы в реальную отправку на плейсхолдер-SMTP (регрессия выключателя). Dev не теряет отладку: дамп под `NODE_ENV==='development'` печатает письмо целиком, включая `Code: …` (~~а также: код лежит в Redis, `forgotPassword:{email}`, TTL 5м, и достаётся через `/get-code/:email`~~ — эндпоинт удалён в SEC-11/L5 2026-08-10; console-дамп остаётся единственным dev-путём и его сознательно сохранили). 2) старт-проверку конфигурации **решено не добавлять** — presence `MAILER_*` уже гарантирован `REQUIRED_SECRETS` (SEC-1); shape-чек (числовой порт) закрывал бы редкий кейс, а вероятная ошибка (опечатка в `MAILER_HOST`) без тестового письма всё равно не ловится → выбран presence-only + падение по факту при отправке (nodemailer `ENOTFOUND`). Ленивый `throw` в `mailer.ts` **закомментирован (не удалён)** как след с отсылкой на `configs/index.ts`; сам `configs/index.ts` не тронут. 3) единый `.env`, `.env.production` не заводим. NB: путь «падение при отправке» опирается на catch в `auth.service`, где висит **FIX-7** (precedence-баг → сообщение «undefined») — отдельный тикет. typecheck зелёный. Закрывает у5:§4.2 | у5:§4.2 |
| SEC-6 | ✅ Код сброса пароля: `crypto.randomInt`, счётчик неудачных попыток, инвалидация после ~5 промахов | средний | низкая | **сделано 2026-07-21** (`auth.service.ts`, только сервер — контракт ошибок не менялся, клиент не тронут): 1) CSPRNG — `randomInt(10 ** 5, 10 ** 6)` вместо `Math.floor(…Math.random()…)`; форма `10**5/10**6` читаема и чинит off-by-one (верх `1000000` эксклюзивен → диапазон `100000..999999`, 6 цифр). 2) Счётчик — отдельный ключ `forgotPassword_counter:{email}`, засевается `setex(…,300,0)` в `forgotPassword` рядом с кодом (тот же TTL 300с; код кладётся чуть раньше → счётчик всегда переживает код). 3) «Инвалидация» реализована как **лок, а не удаление** (осознанный выбор владельца): при `Number(counter) >= 5` → `cooldownError()` (429), код НЕ удаляется — чтобы cooldown в `forgotPassword` (`if(code) throw`) держал и нельзя было каждым 6-м запросом выбить новый код / продолжить брут; лок живёт до истечения TTL (~5 мин), затем оба ключа гаснут. **Критичный порядок** в `resetPasswordWithCode`: shape-check `Number(code)>=10**5 && <10**6` → `get` кода → `if(!codeStr) throw` (иначе `incr` на публичном роуте создавал бы **TTL-less** ключ-счётчик для любого email — утечка памяти + пред-лок; ловили при ревью) → counter-check → `incr` **только** в ветке промаха (верный код не жжёт попытку). Успех чистит оба ключа. Порог ровно 5 (`>= 5` + инкремент после проверки). Оракула нет: «нет кода»/«неверный код» → одинаковый `Invalid code` (400). typecheck зелёный, проверено в Docker. Усиливается SEC-7 (rate-limit). Закрывает у3:M2 | у3:M2 |
| SEC-7 | ✅ Жёсткие пер-роутные rate-limit на `/login`, `/forgot-password`, `/reset-password-code`, `/check-email`; общий дефолт (1000/мин) снизить | средний | низкая | **сделано 2026-07-22** (`app.ts` + `auth.routes.ts`, только эти два файла — контроллер/сервис/клиент не тронуты): глобальный дефолт `app.register(rateLimit)` → явный `{ max: 500, timeWindow: '1 minute' }` (было неявно 1000 из дефолтов плагина). Пер-роутные оверрайды через `config.rateLimit` (мёржится с глобальным конфигом для конкретного пути, не складывается поверх него) — хелпер `configRL(count) => ({ config: { rateLimit: { max: count, timeWindow: '1 minute' } } })`: `/login` 20/мин, `/forgot-password` 10/мин, `/reset-password-code` 5/мин, `/check-email` 30/мин. `/registration` вне scope источника — не тронут. Дефолтный `keyGenerator` — `req.ip` (лимит per-IP, не общий на все клиенты сразу); store — in-memory, не Redis (осознанно: один инстанс сервера и в dev, и в проде, горизонтального масштабирования нет). Static/`upload`/`content`/`storage` под тем же rate-limit хуком (плагин зарегистрирован через `fastify-plugin`, хук глобальный на весь `app`, применяется и к статике/загрузке файлов), но без отдельных пер-роутных лимитов — только под сниженным глобальным дефолтом (осознанно: там легитимно много запросов на одну загрузку страницы). typecheck зелёный. Закрывает у3:M4 | у3:M4 |
| SEC-8 | ✅ Redis: `requirepass` в обоих окружениях; в dev не публиковать порт на хост (или bind 127.0.0.1); в значении refresh-ключа не хранить сам токен | средний | низкая | **сделано 2026-07-23** (только `auth.service.ts` — compose-файлы и `.env.example` не тронуты, **сознательное сужение объёма карточки владельцем**): 1) `requirepass` и закрытие dev-порта **решено не делать**: dev — локальная машина одного разработчика, сеть не входит в модель угроз, а компрометация самой машины не даёт доступа ни к чему ценному; prod — Redis и так недостижим с хоста (`docker-compose.prod.yml`: `expose: ["6379"]`, не `ports:` — порт не публикуется, виден только контейнеру `app` внутри docker-сети). 2) Асимметрия хранения токена устранена: `getAccessAndRefreshTokens` — значение refresh-ключа `redis.setex(..., '')` вместо `refreshToken` (было асимметрично с access-ключом, `''` уже с SEC-4). В `refresh()` проверка отзыва переведена с `redis.get`+truthy на `redis.exists(...) === 0` (симметрично access-guard'у из SEC-4); повторная расшифровка хранимого токена (`jwt.verify(tokenFromRedis)`) убрана как избыточная — ключ адресуется по `uuid` из уже верифицированного входящего токена, при живом ключе значение всегда было байт-в-байт равно присланному `refreshToken`, так что `remember` для новой пары корректно берётся из уже провалидированного `payload`, а не из повторно декодированной копии. Старые сессии (refresh-ключи с полным JWT в значении, выпущенные до деплоя) не ломаются — `refresh()` значение больше не читает, только `exists`. typecheck зелёный (проверено владельцем). Закрывает у3:M5 частично — только хранение токена; `requirepass`/порт вне scope по решению владельца | у3:M5 |
| SEC-9 | ✅ `?showHidden=true` учитывать только для admin | средний | мин | **сделано 2026-07-23** (`content.controller.ts` + `content.repository.ts`, только эти два файла — routes/service/типы не тронуты): выбрано самое дешёвое решение из развилки — `showHidden` **убран полностью**, а не отдан под admin-гейт. Основания: `/data/list` вообще не участвует в публичном рендеринге (секции/страницы идут через `/data/page/:page` и `/data/item/:id`, которые этот параметр не читают) — единственный потребитель списка это админский `pagesData.admin.api.ts:fetchList`, и тот `showHidden=true` никогда не отправлял; ни у одного документа в сидах `hide: true` не стоит, и ни в одной форме админки поле `hide` не выставляется — вся пара «`hide`-флаг + `showHidden`-параметр» была нерабочей с обеих сторон ещё до фикса. В контроллере (`getPageDataList`) параметр `showHidden` убран из query-типа и из вызова сервиса; в репозитории фильтр `filter.hide = { $ne: true }` сделан **безусловным** (было `if (!query.showHidden) …` — при первой правке случайно закомментировали всю строку целиком, из-за чего скрытые документы стали отдаваться вообще всем без каких-либо условий, то есть дыра расширялась, а не закрывалась; поймали при повторном ревью, поправили на безусловное присвоение). Восстановление в будущем (если появится реальная фича «предпросмотр скрытых страниц» в админке) дёшево: вернуть чтение параметра в контроллере + решить гейтинг (мягкая проверка в контроллере vs отдельный роут под `guardAdmin`, как `/data/update/:id`) — репозиторий/типы/seed-поле трогать не придётся. typecheck зелёный. Закрывает у3:M3 | у3:M3 |
| SEC-10 | ~~`forgot-password` — нейтральный ответ независимо от существования email~~ — решено оставить явные ошибки | низкий | низкая | **закрыто без действий 2026-08-10 (решение владельца)**: `forgotPassword` в `auth.service.ts` оставлен как есть — 429 `cooldownError` при уже выданном коде, 404 `notFoundError` для незарегистрированного email, 500 `internalError` при падении mailer'а. Основание: факт «email зарегистрирован» и так раскрывается **осознанно** через `GET /api/auth/check-email/:email` (проверка занятости email в форме регистрации — рабочая фича, в scope SEC-10 не входила), поэтому 404 здесь не даёт атакующему информации, которой у него ещё нет; при этом все три ошибки нужны живому пользователю — опечатался в email, нажал «выслать код ещё раз» до истечения 5 минут, письмо не ушло (не ждать его зря). Клиент менять не потребовалось: `useErrorMessage.ts` резолвит `errors.forgotPassword.NOT_FOUND` (scoped) → `errors.ACTION_COOLDOWN`/`errors.INTERNAL_ERROR` (общие), переводы уже есть в `ru.json`/`en.json`, `ForgotPassword.vue` выводит их одной catch-all веткой. Остаточный тайминг-оракул (`await mailer` ждёт SMTP только в ветке «юзер есть») сознательно не трогаем. `resetPasswordWithCode` уже без оракула — порядок проверок закреплён в SEC-6 | у3:M6 |
| SEC-11 | ✅ Хвосты: `KEYS`→`SCAN` в logoutAll (L3), CORS (L4), удалить `get-code/:email` (L5) | низкий | низкая | **сделано 2026-08-10** (`auth.service.ts`, `app.ts`, `auth.routes.ts`, `auth.controller.ts`; тремя отдельными правками). **L3** — `redis.keys()` в `logoutAll` заменён на курсорный цикл: `do { const [next, keys] = await redis.scan(cursor,'MATCH',pattern,'COUNT',100); cursor = next; if(keys.length) await redis.unlink(...keys) } while(cursor !== '0')`. Мотив **не в скорости**: `SCAN` делает ту же полную работу (индекса по именам ключей в Redis нет, обход всей базы никуда не девается), но порциями — между вызовами однопоточный Redis успевает обслужить чужие команды, тогда как `KEYS` держал его целиком на всё время обхода. Выигрыша по времени нет и не ожидалось; на текущей базе (только токены + коды сброса; rate-limit store in-memory, не Redis) эффект нулевой — правка сделана из-за формы: `logoutAll` дёргает любой авторизованный юзер (пер-роутного лимита нет, только глобальные 500/мин из SEC-7), а стоимость `KEYS` растёт с размером всей базы, а не с числом совпадений. Грабли учтены: выход строго по `cursor === '0'` (пустая порция при живом курсоре штатна — `MATCH` фильтрует уже выбранное, выход по пустому результату потерял бы токены), `if(keys.length)` **внутри** цикла (`unlink()`/`del()` без аргументов бросают), `COUNT` — подсказка, а не лимит, дубли в выдаче безвредны (повторный `unlink` — no-op). `del`→`unlink` (в compose `redis:7-alpine`, команда с 4.0) — освобождение памяти в фон. Инвариант, который нельзя ломать при будущих правках: в `changePassword` `logoutAll` вызывается **до** `getAccessAndRefreshTokens`, иначе цикл снесёт только что выданную пару. `redis.keys` в `server/src` больше не встречается нигде. **L4** — `app.register(cors)` → `app.register(cors, { origin: false })`. Whitelist со «своим доменом» сознательно **не** заводили: кросс-origin запросов в проекте не бывает (dev — браузер знает только vite `:5173`, `/api` и `/upload` уходят через прокси серверно; prod — Fastify сам раздаёт собранный клиент статикой с того же адреса; `VITE_API_URL` не задан, фолбэк `'/api'` относительный), а «на будущее» whitelist без `credentials:true` **и** переезда refresh-cookie с `sameSite:'lax'` на `'none'` всё равно не заработает — вышла бы ложная надпись «CORS настроен» на неработающей конфигурации. Дыры до правки не было: с дефолтным `origin:'*'` сервер не отдаёт `Access-Control-Allow-Credentials`, и браузер credentialed-ответ отбрасывает сам (поправка к у3:L4, где сказано, что credentials выключены — клиент как раз шлёт `credentials:'include'` на каждом запросе, `fetcher.ts`). Новых env не заводили: `REQUIRED_SECRETS` (SEC-1) — presence-check с fail-fast, добавление туда `CORS_ORIGIN` уронило бы каждый существующий `.env` и любой форк ради low-находки. **L5** — `GET /get-code/:email` удалён целиком: роут + `guardAdmin` из импорта `auth.routes.ts` (в файле больше не использовался) + `getCode` в контроллере и в сервисе. Мотив: отдавал сырой активный код сброса и был единственным обходом ограничения, наложенного на роль admin — сменить чужой пароль штатно админ не может (`buildUpdate` в `users.service.update` — whitelist без `password`, ветка с `bcrypt.hash` закомментирована), а цепочкой `forgot-password` → `get-code` → `reset-password-code` мог, для любого аккаунта включая другого админа. Клиент эндпоинт не использовал (грепом по `*.ts`/`*.vue`/`*.json` — только серверные файлы). Dev-отладка не потеряна вопреки примечанию в SEC-5: заглушка `mailer.ts` печатает письмо целиком, включая `Code: …`, при `MAILER_IS_ACTIVE!=='true' && NODE_ENV==='development'` — в дефолтном dev код и так виден в логах контейнера. Примечание карточки «L3 упрощается при SEC-4-A (SET токенов юзера)» оказалось **устаревшим**: SEC-4 сделан вариантом A *без* SET (guard проверяет конкретный ключ `user:{id}:token:access:{uuid}`), per-user SET в коде нет и заводить его ради low-находки не стали — это правки в выпуске токенов, logout, refresh и guard. Закрывает у3:L3, L4, L5 | у3:L3-L5 |

## Этап 2 — корректность: маленькие правки с реальными багами (FIX)

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| FIX-1 | ✅ Потерянные await/проглоченные ошибки: `await` в `folders.service.delete`; логировать причину в catch `files.service`/`profile.service`; `parseAuthors` — не глотать битый JSON (400 вместо пустого списка) | средний | мин | **сделано 2026-08-10** (`folders.service.ts`, `auth.service.ts`, `files.service.ts`, `profile.service.ts`, `reports.controller.ts` — только сервер, клиент не тронут). **A. Потерянный `await`** (`folders.service.delete`): `repo.delete(id)` → `const result = await repo.delete(id); if (result.deletedCount === 0) throw conflictError('Folder cannot be deleted')`. Мотив жёстче, чем «теряется ошибка»: `process.on('unhandledRejection')` в `server/src` нет (проверено грепом), а Node ≥15 по умолчанию валит процесс на необработанном реджекте → отвалившаяся Mongo на удалении папки роняла сервер уже **после** отправки клиенту 200. Проверка `deletedCount` взята сверх минимума — для единообразия с `create`/`update` в том же файле (оба проверяют результат репозитория и бросают `conflictError`) и закрывает окно гонки между `findById` и `delete` (между ними `await Promise.all` на детей). **D. Тот же класс, в карточке не назван** (`auth.service.resetPasswordWithCode`, был в источнике у4:срез 2 как `auth.service.ts:61`): два fire-and-forget `redis.del('forgotPassword:'+email)` / `del('forgotPassword_counter:'+email)` свёрнуты в один `await redis.del(k1, k2)` (вариадичный DEL ioredis, один round-trip вместо двух — решение владельца, вариант «просто два `await`» отвергнут как более многословный при равной цене). Контракт не меняется ни в одном сценарии: следующая же строка `await this.logoutAll(...)` ходит в Redis (`scan`/`unlink`), так что при лежащем Redis запрос падал в 500 и раньше — правка убирает только окно unhandledRejection. `await` при этом **не чинит** живучесть кода сброса: если Redis недоступен, ключи доживут свой TTL 300с в любом случае, правка лишь делает это видимым. NB на будущее: multi-key `DEL` требует одного слота в Redis Cluster — сейчас один инстанс в docker-сети, неактуально. **B1. `files.service.delete`** — `try/catch` с `throw internalError()` снят **целиком**, логирование в сервисе не добавлялось. Основание: формулировка источника «не логируется и не попадает в errorHandler» неточна — подменённая ошибка долетала и логировалась, `request.log.error(error)` стоит первой строкой `errors.handler.ts`, до всех веток; терялась именно **причина**. Ветка «неизвестная ошибка» отдаёт тот же 500 `INTERNAL_ERROR`, что и `internalError()` → тело ответа не изменилось побайтово, а в лог вместо пустышки попадает настоящая ошибка со стеком. Реальный источник реджекта в `try` был ровно один — `filesRepo.delete` (Mongo): `fsStorage.deleteFile` глушит **все** fs-ошибки внутри себя (`.catch(() => {})` на `rm`/`unlink`/`readdir`/`rmdir`), т.е. catch ловил почти исключительно сбой Mongo и выдавал его за анонимный 500. Отвергнуто: тащить `app.log` параметром фабрики (`createFilesService(filesRepo, foldersRepo)` — логгера в скоупе нет, а errorHandler и так всё пишет); прокидывать причину в `internalError(String(error))` — `message` AppError уходит клиенту в теле ответа, это утечка внутренностей Mongo; добавлять `cause` в `AppError` — он **плоский объект** `{message, statusCode, code}` (`errors.factory.ts`), без стека и `cause`, правка типа+фабрики+handler'а вне границ карточки. Импорт `internalError` в файле стал неиспользуемым и убран. **B2. `profile.uploadAvatar`** — тот же паттерн, но механически снять `try/catch` было нельзя (подменялось на 409): `try { await repository.update(...) } catch { throw conflictError('User not updated') }` → `const updatedUser = await repository.update(...); if (!updatedUser) throw conflictError('User not updated')`. Разбор показал зеркальную пару дефектов: catch ловил **только** инфраструктурный сбой (`update` = `updateOne`+`findOne`; невалидный ObjectId исключён — id из `user._id.toString()`, юзер прочитан строкой выше) и выдавал 500-ситуацию за 409, т.е. «поправь запрос и повтори» на неустранимую клиентом проблему; а настоящий случай «не обновилось» (`update` вернул `null` — юзера удалили между `findById` и `update`) не проверялся **вовсе** и давал 200 с путём к аватарке. После правки: сбой Mongo → 500 с причиной в логе, исчезнувший юзер → 409, поведение симметрично `updateProfile` в том же файле. Клиент не тронут и не ломается: `ProfileUploadAvatar.vue` вызывает `uploadAvatar` без `try/catch`, код ошибки не разбирает, `isConflict` на этом экране не используется. **C. `parseAuthors`** (`reports.controller.ts`, вызовы из `create` и `update`) — `catch{}` заменён на 400: `JSON.parse` в узком `try` → `throw validationError('Authors must be a valid JSON array')`, следом `if (!Array.isArray(authorsJson)) throw validationError('Authors must be an array')`; `validationError` импортирован в контроллер (прецедент — `profile.controller.ts`). 400 безопасен: легитимный клиент всегда шлёт `JSON.stringify(ReportAuthor[])` (`useBuildReportFormData.ts`), т.е. битый JSON приходит не от браузера; а сервис `authors` не валидирует (`reports.service.ts`, `authors: authors ?? null`) → тишина доезжала до БД и доклад сохранялся без авторов. Бросок оставлен **внутри** `for await (const part of parts)` (fail-fast) осознанно: в FormData `authors` идёт раньше файла, поэтому тело до конца не докачивается и клиент может получить обрыв вместо аккуратного 400 — размен принят, т.к. вариант «собрать флаг и бросить после цикла» заставлял буферизовать весь файл (`part.toBuffer()`) ради заведомо невалидного запроса, что ещё и усиливало бы «мусор + большой файл». **Остаточное (сознательно → VAL-1)**: элемент-не-объект внутри массива по-прежнему даёт «авторов» из пустых строк (`name \|\| ''`), а `null`-элемент бросает `TypeError` при деструктуризации → теперь 500 вместо прежнего молчаливого обрезанного списка; строгий 400 на это — одна строка-гард, но это уже валидация схемы (zod, VAL-1). **Остаточное, вне карточки**: порядок «сначала диск, потом БД» в `files.delete` и `profile.uploadAvatar` не менялся — при сбое Mongo остаются висячий файл/мусорная папка аватарки; `try/catch` этого не чинил никогда, теперь такие случаи хотя бы видны в логах | у2:п.10, у4:срез 2 |
| FIX-2 | ✅ Хелпер `toObjectId(id)` с `validationError` — во все репозитории (9 мест конструируют `new ObjectId` без проверки → 500 вместо 400) | средний | низкая | **сделано 2026-08-10** (новый `server/src/common/utils/toObjectId.ts` + 5 репозиториев: users, reports, folders, files, content — сервисы/контроллеры/клиент не тронуты). **Хелпер**: `toObjectId(id: string, label = 'Id'): ObjectId` — внутри `ObjectId.isValid(id)`, при провале бросает `validationError` с текстом `${label} is not valid` (400 VALIDATION_ERROR), иначе возвращает `new ObjectId(id)`. Форма выбрана **бросающая, а не булев guard**: guard требует `if`+`throw` на месте вызова, то есть однострочные стрелки `findById`/`delete` пришлось бы разворачивать в блок — ровно поэтому они и оставались непокрытыми, пока соседние методы в тех же файлах проверку имели. Второй аргумент `label` сохраняет уже существовавшие именные тексты (`'Folder id'`, `'File id'`, `'Parent id'`, `'User id'`, новый `'Page id'`) — благодаря ему перевод старых проверок на хелпер не изменил ни одного ответа API. **Счёт из карточки не сошёлся** (та же история, что с забытым `redis.del` в FIX-1): в карточке «9 мест», в источнике у4:4b перечислено 13 конструкций в 11 методах, грепом на 2026-08-10 нашлось **14 в 12 методах** — не был назван `reports.repository.ts:19` (`userId` из query-строки `/api/reports/list?userId=`, доступен admin и vereficator). **Охват — полный** (решение владельца): переведены не только 14 непокрытых мест, но и 13 уже написанных вручную `if (!ObjectId.isValid(x)) throw validationError(...)` — всего 27 вызовов, в коде остался один способ вместо двух похожих (ради этого карточка и заведена, у4 — документ про «одна задача решена 2–3 способами»). После правки `new ObjectId` в `server/src` встречается ровно дважды: внутри самого хелпера и `mongodb.init.ts:29` (литерал корневой папки, не пользовательский ввод — сознательно не трогали). Импорты `ObjectId` и `validationError` в репозиториях стали неиспользуемыми и убраны. **Уровень проверки — репозиторий** (решение владельца): формат `:id` по-хорошему валидация входа, но zod из VAL-1 «неясно когда будет», а репозиторий единственный, кто реально конструирует `ObjectId`, и защищён от любого вызова, включая внутренние сервисы. При VAL-1 не дублировать проверку в третьем месте. **`ObjectId.isValid` оставлена как есть**, до 24-hex regex не ужесточали (решение владельца): она пропускает любую 12-символьную строку (`abcdefghijkl` — валидные 12 байт), такой id станет реальным `ObjectId` и даст 404/пустой список вместо 400; строгий формат — при желании одна строка внутри хелпера. **Опциональные id — без отдельного `toObjectIdOptional`**: обычный `if (x)` на месте вызова, внутри обычный хелпер («пришёл непустой — обязан быть валидным»). **Смена поведения, согласована**: `folders.repository.update` раньше молча игнорировал битый `parentId` (`ObjectId.isValid(parentId+'')` внутри спреда `$set`, `+''` стоял ради `undefined`) — теперь 400; `''`/`null`/`undefined` по-прежнему означают «не менять поле» (`folders.service` мапит `undefined→null`, `buildUpdate` режет `null`, пустая строка доезжает до репозитория и отсекается проверкой на истинность). Метод перестроен на предсобранный `updateData`, как в `files.update`; в `update`-методах всех репозиториев `_id` теперь считается один раз вместо двух вызовов `new ObjectId(id)`. `reports.findList` с `?userId=abc` — 400 вместо 500. **Ловушка модуля content учтена**: хелпер применён только к `updatePageData`, а `getPageDataById`/`getConfigById`/`getPageDataByPage` читают по строковому слагу (`findOne({ id, lang })`), не по `_id` — сплошная замена по имени переменной сломала бы публичное чтение контента. Отвергнуто: добавить ветку для `BSONError` в `errorHandler` — дешевле на вид, но прячет проблему вместо валидации входа. Проверено, что 400 из репозитория долетает до клиента: `catch` по всем `*.service.ts` есть только у mailer'а, jwt-verify и файлового отката в `files.service` (тот делает `throw error` дальше), ни один сервис вызовы репозитория не оборачивает. `tsc --noEmit` зелёный | у2:п.11, у4:4b |
| FIX-3 | ✅ Нормализация email (`trim().toLowerCase()`) в `users.service.create` | средний | мин | до UNI-2. **Сделано 2026-08-11** (коммит `fb08468`: `auth.service.ts`, `users.service.ts` на сервере + `auth.store.ts`, `useForgotPassword.ts` на клиенте). **Форма — локальная функция в каждом из 4 файлов**, а не общая утилита в `common/utils` (сознательное отступление от прецедента `toObjectId` из FIX-2, решение владельца): 5 точек нормализации на 2 серверных файла — отдельная сущность оверкил, а после UNI-2 лишняя копия удалится вместе со слитым методом. Сигнатура `(email?:string) => (email ?? '').trim().toLowerCase()` — не косметика: нормализация встала **до** гварда `!email`, а тело запроса не валидируется (VAL-1), поэтому на `undefined` голый `.trim()` дал бы 500 на кривом теле вместо честного 400. **Объём шире карточки** (в карточке — только `users.service.create`): нормализация есть теперь во всех 6 методах, работающих с email — `forgotPassword`, `resetPasswordWithCode`, `validateUser`, `checkEmail` (там уже была — переведена на хелпер и поднята выше гварда), `registration`, `users.service.create`. Основание: запись уже нормализовалась, а чтение нет — само расхождение и есть баг. **Главный живой баг — регистрозависимый логин**: клиент опускал регистр в `register` (`auth.store.ts:155`), но не в `login`, а `validateUser` искал точным совпадением → человек, зарегистрировавшийся как `Alex@Mail.ru`, той же строкой получал 401 «неверный логин или пароль» на верных данных. **`registration`**: проверка занятости шла по сырому (`:245`), вставка по нормализованному (`:259`) → дубль в другом регистре проскакивал проверку и упирался в unique index, `E11000` драйвера не наш `AppError` → 500 вместо `conflictError` 409; отмечено, что худший исход давала именно **половинчатая** нормализация (не нормализовать вообще — вставка проходит, нормализовать оба конца — чистый 409). Исправлено выносом `email` из деструктуризации: `const email = normalizeEmail(user.email)` до гварда, дальше одна переменная и на проверку, и на запись. **`users.service.create`**: оба конца были сырые → админ мог завести неотличимого двойника (unique index регистрозависим, `User@Mail.ru` при живом `user@mail.ru` проходит). Порядок шагов выровнен с `registration` (нормализация → гварды → проверка занятости → хеш → сборка) — чтобы UNI-2 свёлся к удалению дубля, а не к сверке двух политик; `email` в литерале стоит **после** `...data`, иначе спред затирает нормализованное значение обратно. **Критичный порядок в forgot/reset** (иначе правка ухудшала бы систему): нормализация сделана один раз в начале метода, в переменную, и из неё строятся и `findByEmail`, и все 7 подстановок в Redis-ключи (`forgotPassword:{email}`, `forgotPassword_counter:{email}`), и адрес письма. Раньше сырые ключи были безопасны ровно потому, что поиск тоже сырой — чужой регистр умирал на `notFoundError` до создания ключей; с нормализованным поиском и сырыми ключами каждый новый регистр давал бы свежий кулдаун и свежий счётчик, то есть обход лока из SEC-6: неограниченный перебор шестизначного кода плюс рассылка писем жертве. **Гвард на пустоту** (строка из пробелов непустая, после `trim()` становится `''`) поставлен только там, где он что-то меняет: в `users.service.create` (гварда на email не было вовсе — мусор доезжал до БД, а unique index пропускал ровно одну такую запись) и в `checkEmail` (гвард стоял **до** нормализации, из-за чего строка из пробелов давала форме `{available:true}`). Читающие `validateUser`/`forgotPassword`/`resetPasswordWithCode` не тронуты: их 401/404/`Invalid code` корректны, перевод на 400 менял бы контракт ради уже обработанного случая → VAL-1. **Клиент** (решение владельца — «нормализовать и там и там», страховка от будущих фич): локальные копии хелпера в 2 файлах, `login` собирает `loginData` с нормализованным email, `register` переведён с инлайна на хелпер, `forgotPassword`/`setNewPasswordWithCode` — то же; функционально после серверной правки клиент уже ничего не чинит. **Не делалось**: нормализация существующих документов и collation-индекс `{locale:'en',strength:2}` — решение владельца «база сейчас болванка, реальных данных нет»; сменить опции существующего unique-индекса всё равно нельзя (`IndexOptionsConflict`, код 85 — нужен drop+create, и при живых двойниках по регистру индекс просто не соберётся), то есть это отдельная миграция, а не строка в `mongodb.init.ts`. Формат email и схемы роутов — VAL-1; слияние `registration` и `create` — UNI-2 (FIX-3 сознательно только подготовил почву); `check-email` по логике не менялся; `reports` (`authors[].email`, `$regex`-фильтры) не тронут. **Смежная находка вне scope**: `admin.users.api.ts:9 createUser` шлёт `FormData`, а `users.controller.create` читает `request.body` как JSON — расхождение реальное, но метод не вызывается ни из одного `.vue` (админской формы создания пользователя пока нет). Оба typecheck зелёные (`tsc --noEmit` + `vue-tsc --noEmit`) | у2:п.4 |
| FIX-4 | ✅ Семантика `roles: []` в клиентском guard'е; страница `/guard`; явные `roles` в meta `/admin/users` и `/admin/reports` | средний | низкая | **сделано 2026-08-11** (`routes.guard.ts`, `routes.ts`, `router.types.ts` — только клиент; сервер в этой задаче эталон и не тронут). **Развилка 2 решена ПРОТИВОПОЛОЖНО тексту карточки** (решение владельца при разборе): семантику `roles: []` к серверной **не приводили**, клиентское «не пускать никого» оставлено и зафиксировано комментарием в `RouteMeta`. Основание: на сервере семантики пустого массива нет вовсе — `authRoleGuard` совмещает авторизацию и роли в одной функции, и `!roles || roles.length === 0` (`authRole.guard.ts:27`) обрабатывает **отсутствие аргумента** (`requireAuth = authRoleGuard()`), вызова `authRoleGuard([])` в коде нет ни одного; на клиенте же `auth` и `roles` — два независимых ключа meta с двумя независимыми блоками в guard'е, поэтому серверное прочтение сделало бы `roles: []` точным синонимом отсутствия ключа, то есть синтаксисом-пустышкой, и отняло бы единственное значение, которое там что-то выражает. Расхождение переквалифицировано из бага в незадокументированное различие, риск карточки («первая же настоящая запись `roles: []` будет означать не то, что имел в виду писавший») снят комментарием, а не переворотом кода. **Страница `/guard` НЕ удалена** (второе отступление, решение владельца): оставлена как канарейка — проверка, что guard действительно отказывает; `roles: []` там осмысленное значение и единственный носитель. **A. Авторизация выводится из наличия ролей**: `const isAuthPage = to.meta.auth === true || allowedRoles !== undefined`. Чинит вторую половину условия ролей `if (allowedRoles && authStore.user)`, где при `user === null` проверка ролей **пропускалась целиком** — роут с `roles`, но без `auth: true`, пустил бы гостя. Условие оставлено как есть (не переписано на `?.`) осознанно: `isAuth` — computed от `user` (`!!user.value`, `auth.store.ts:65`), то есть после правки до блока ролей неавторизованный не доходит вовсе и `authStore.user` там не null по построению; в коде это закреплено комментарием. Поведение сегодня не изменилось — все три носителя `roles` (`admin-layout`, `/test`, `/guard`) уже несли `auth: true`. **C. Явные роли**: до правки собственных `roles` у `/admin/users` и `/admin/reports` не было вовсе — оба наследовали от `admin-layout` `['admin','manager','vereficator']`, то есть список задавался в третьем месте и по случайности. Сначала по развилке 3 проставлено `['admin', 'vereficator']` (совпадает с сервером — `requireVereficator` на `/users/item/:id` и `/users/list` — и с навигацией `AdminLayout.vue:36,45`), **но сразу после этого владелец вернул `manager` в оба списка** и оставил его же на `/admin/pages-data`. Итоговое состояние в коде: `roles: ['admin','manager','vereficator']` на users и reports, `['admin','manager']` на pages-data. Решение сознательное и зафиксировано владельцем так: «пусть видит, если он что-то видит, а сделать не может — это нормально; интерфейс будет переделываться с нуля». Что при этом остаётся известным и принятым: manager на `/admin/users` получит 403 на `/users/list` (страница откроется без данных); на `/admin/pages-data` откроет редактор и получит 403 при сохранении (единственная запись модуля content — `PATCH /data/update/:id` под `guardAdmin`, `content.routes.ts:21`); на `/admin/reports` получит **200 со списком своих собственных докладов** под админским заголовком (SEC-3 подставляет `userId = свой` всем, кроме admin/vereficator) — не отказ, а тихо неверные данные. Ценность правки не в самих списках, а в том, что теперь они у роутов **свои и явные**, а не унаследованные: менять их можно в одном месте, не задевая соседей. **D. `roleRedirect`**: форма ключа **не менялась** (владелец рассматривал карту «роль → страница» вместо одного значения и от неё отказался). `admin-layout` получил `roleRedirect: { name: 'profile' }` — vue-router склеивает meta по цепочке matched, поэтому ключ наследуется детьми без своего и работает терминальным фолбэком (профилю роли не нужны, цепочка отказов обрывается); `/admin/users` и `/admin/reports` получили свой `{ name: 'admin' }`, так что manager остаётся в админке — `/admin` разворачивается в `admin-pages-data`, куда он проходит. **Ловушка, из-за которой фолбэк обязателен**: `roleRedirect` у pages-data и files уже указывает на `admin-reports`, поэтому обратная стрелка reports → `/admin` (→ pages-data) замыкает кольцо; в него попадала бы роль `user`, набравшая адрес админки руками — список ролей `admin-layout` её не останавливает, потому что собственный `roles` ребёнка перекрывает родительский целиком, а guard читает только склеенное значение (то есть родительский список вообще не защищает детей со своими ролями). Сам vue-router такое кольцо не обрывает: защита в `pushWithRedirect` (`vue-router.mjs:1307`) считает повторы только когда guard возвращает **ту же** локацию, куда шла навигация, и вдобавок обёрнута в `NODE_ENV !== 'production'` — кольцо из трёх разных адресов проходит мимо неё и в проде раскручивается до переполнения стека. Отдельно проверено по исходнику (`vue-router.mjs:1288-1293`): `handleRedirectRecord` срабатывает **до** `navigate()`, запись-редирект `/admin` в итоговый `matched` не попадает и её meta не читается — вешать `roleRedirect` на неё было бы мёртвым кодом. **E. Типы**: `roleRedirect?: RouteLocationRaw` объявлен в `RouteMeta`, каст на нём в guard'е снят, импорт `RouteLocationRaw` переехал из guard'а в типы; каст у `roles` тоже убран как избыточный. **F. Найдено при инвентаризации, сознательно вне границ карточки**: 1) `/admin/pages-data` и `/admin/files` — meta `['admin','manager']` против меню `checkRole(['admin'])`, причём страницы ходят в **разные** модули и требования сервера у них противоположные: files → storage, весь модуль под `requireManager` (`storage.routes.ts:17`), manager полноправен и меню прячет пункт зря; pages-data → content, запись под `guardAdmin` (`content.routes.ts:21`), manager сохранить не сможет и права в meta ему выданы зря. То есть «правильный» список тут разный для двух соседних роутов. Не чинили: меню владелец назвал черновиком и правит сам, а несоответствие meta принято сознательно (см. решение в блоке C). 2) Кнопки удаления и сохранения в `AdminUserCard.vue:102,113` и `AdminReportCard.vue:86,95` гейтятся по `['admin','manager']` — третий по счёту список, не совпадающий ни с одним серверным (users update/delete под `requireAdmin`; reports — владелец-логика SEC-3, причём vereficator'у пишется только `status`). Живой дыры нет: manager до обеих страниц больше не доходит, vereficator в список не входит и лишних кнопок не получает, а статусные «Одобрить»/«Отклонить» (`AdminReportCard.vue:93-94`) не загейтены и ровно совпадают с его серверным правом — то есть исход сегодня верный случайно, при первом изменении ролевой модели расклеится. 3) `checkRole` (`auth.store.ts:183`) считает гостя ролью `user` (`user.value?.role \|\| 'user'`) — сегодня безвредно, ни один из семи вызовов `'user'` в списке не передаёт. **Не делали по границам карточки**: серверные guard'ы и роуты, ролевую модель, `/test` (отладочный, но роли честные), i18n попапов guard'а (UNI-9), закомментированные роуты (DX-8), переименование `vereficator` (MIG-1). `pnpm run type-check` (`vue-tsc --build`) зелёный. Код применял Claude по просьбе владельца, коммит за владельцем | у4:срез 7 |
| FIX-5 | `changePassword`: runtime-проверка `type === 'refresh'` после verify; `accessToken!` в logout → явный 401 при битом заголовке | средний | низкая | — | у5:§3.5, §3.7 |
| FIX-6 | Единый контракт удаления: везде 204 без тела (эталон users) + клиентские delete-методы типизировать `Promise<void>` (fetcher на 204 возвращает null) | средний | низкая | сервер+клиент одним коммитом; вместе с UNI-7 | у4:срез 2, у5:§3.2 |
| FIX-7 | `catch (e: any)` + precedence-баг `e?.message+'' \|\| 'Mailer error'` в auth.service (ошибка «undefined») | низкий | мин | — | у5:§5 |
| FIX-8 | Текст ошибки mongodb-плагина — настоящие имена переменных (`MONGO_DB_CONNECT`/`MONGO_DB_NAME`) | низкий | мин | — | у5:§4.1 |
| FIX-9 | Отдельный маленький лимит на аватар (~5MB) + проверить пережатие после загрузки | средний | низкая | — | bl:#2 |
| FIX-10 | Пул/лимит sharp-воркеров (1–2) и/или rate-limit на `/upload/images/*` | средний | средняя | — | bl:#1 |
| FIX-11 | ✅ Самопубликация доклада владельцем (`status` пишется кем угодно на своём докладе) + роль manager в reports | высокий | низкая | **сделано 2026-08-11, вне очереди** (`reports.controller.ts`, `reports.service.ts`) — найдено при разборе FIX-4, в исходных срезах у2–у5 этой находки не было. **Дыра**: `PATCH /reports/update/:id` висит под `requireAuth`, контроллер принимал любой из четырёх статусов от кого угодно, а сервис писал `status` владельцу доклада независимо от роли (`admin` или владелец → общая ветка `buildUpdate` со `status`) — то есть любой пользователь мог опубликовать собственный доклад одним запросом, минуя проверку. Через UI обычному юзеру кнопок не дают, но API был открыт. Показательно, что на **создании** это было закрыто (`reports.controller.ts:75,91-92` — дефолт `draft`, из формы принимаются только `draft`/`waiting`): защита задумывалась, но на update её не сделали. **Правка 1, контроллер** (`:135`): `published`/`rejected` принимаются только у `['admin','manager','vereficator']`, `draft`/`waiting` — у всех. Роль берётся из `request.user.role`, то есть из токена, а не из части multipart — подделать полем формы нельзя. Отказ **молчаливый** (ключ просто не попадает в payload, `buildUpdate` его выбрасывает, ответ 200 со старым статусом) — решение владельца: легитимный клиент такого запроса не шлёт, обрабатывать ошибку ради взломщика смысла нет; форма совпадает с уже принятым стилем `create`. **Правка 2, сервис**: без неё правка 1 оставляла бы manager'у ровно самоодобрение и ничего больше — сервис не считал его привилегированным нигде. Заведены константы модуля `MODERATOR_ROLES = ['admin','vereficator','manager']` (видят чужие доклады и модерируют) и `STATUS_ONLY_ROLES = ['vereficator','manager']` (в чужом докладе пишут только статус) — набор нужен в трёх местах, инлайн-литералы разъехались бы. `findList`: подстановка `userId = свой` теперь не применяется ко всем модераторам (было `['admin','vereficator']`) → manager видит все доклады; этим же закрыта первая находка FIX-4, где админская страница докладов молча отдавала manager'у его собственные доклады под админским заголовком. `update`: допуск к чужому докладу по `MODERATOR_ROLES`; ветка «только статус» теперь срабатывает **только на чужом** докладе (`STATUS_ONLY_ROLES.includes(role) && report.userId.toString() !== performedBy._id`) — до правки она смотрела лишь на роль, из-за чего vereficator не мог отредактировать текст собственного доклада, а после добавления manager'а это распространилось бы и на него. `delete` не трогали: удаление чужого доклада по-прежнему только у admin. **Осознанно оставлено**: самоодобрение для admin/manager/vereficator (они и есть те, кто проставляет статусы); `GET /reports/item/:id` без проверки владельца — **не IDOR по решению владельца**, доклады считаются публичными для авторизованных, любой может прочитать чужой; молчаливое игнорирование запрещённого статуса. **Хрупкое место на будущее**: проверка прав на статус живёт на слое разбора запроса (контроллер), а не там, где есть `performedBy` — при UNI-1 (общий `parseMultipart`) и VAL-1 (zod-схемы) этот слой переписывается, и проверку легко потерять; в сервисе рядом с допуском оставлен комментарий-ссылка на строку контроллера. Код писал владелец, `pnpm run typecheck` зелёный | найдено при FIX-4 |

## Этап 3 — валидация входа (VAL)

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| VAL-1 | Zod-схемы на вход, поэтапно: **шаг 1** — auth-роуты (login/registration/reset: поля-строки → закрывает NoSQL-инъекцию) и `reports.list` (escape/лимит `$regex`, max `limit`); **шаг 2** — остальные модули | высокий | средняя-высокая | zod уже в deps — **не удалять** в DX-1; после GT-2; частично пересекается с SEC-3 (потолок limit) | у3:M1 |

## Этап 4 — унификация по эталонам из у4 (UNI)

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| UNI-1 | Общий `parseMultipart()` в `common/`: стриминг в fsStorage без 100MB-буфера, честные Body-типы роутов, убрать `as typeof file.file` / `as keyof` / `part.value as string` (5 копий цикла → 1) | высокий | средняя | до UNI-4; закрывает у5:§3.3 и половину инвентаря у5:§5 | у2:п.3, у4:срез 5 |
| UNI-2 | `auth.registration` → общий `users.service.create` (одна точка создания пользователя) | средний | средняя | после FIX-3 и SEC-2 | у2:п.4 |
| UNI-3 | Пути файлов: единый модуль `uploadRoot` / `toDiskPath()` / `toUrlPath()`; в БД остаются URL-пути с `/upload` (развилка 4); заодно чинит `.replace('upload','')` в upload.controller | средний | средняя | — | у2:п.7, у4:срез 7, bl |
| UNI-4 | `buildUpdate` строится один раз в сервисе (files, reports); клиентский двойник `useBuildUpdate` → `shared/` | средний | низкая | после UNI-1 | у4:4c |
| UNI-5 | Пагинация: единый контракт `{list, count}` (эталон users) + дефолтный `limit` у users; reports без переупаковки `total→count` | средний | низкая-средняя | согласовать shared-типы с клиентом (GT-1) | у4:4a |
| UNI-6 | Guard-стили по эталону reports (addHook + вложенный register); переименовать `storage.contraller.ts` и опечатки идентификаторов (serivice, contraller) | низкий | низкая | не трогать `vereficator` — это MIG-1 | у4:срезы 3, 9 |
| UNI-7 | Статусы успеха: `return`/201/204 по users-эталону во всех контроллерах | низкий | низкая | вместе с FIX-6 | у4:срез 2 |
| UNI-8 | Клиент: единый `reports.api` (+ перенос useReports из profile), мутации через `invalidateQueries`, единый контракт ошибок композаблов, query-string только через fetcher (`&page=undefined`-баг уйдёт) | средний | средняя | после GT-1 | у2:п.8, у4:срез 6 |
| UNI-9 | Пользовательские сообщения и meta-заголовки — через i18n вместо хардкода русского | низкий | средняя | после GT-1; удобно вместе с UNI-8 | у4:срез 6 |

## Этап 5 — миграции данных (MIG) — решения приняты, выполнять после стабилизации

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| MIG-1 | `vereficator` → `verificator`: миграция документов users + константа shared, guard'ы, роуты, клиентский router/nav, i18n — одним коммитом со скриптом миграции | средний | средняя | после SEC/FIX-этапов; тянет правки в местах UNI-6 — координировать | у4:решение 1 |
| MIG-2 | `initionalValues` → `initialValues`: formConfigs пересоздаются сидами (миграция почти бесплатная) + типы shared, сиды, клиентские композаблы, skill-доки | низкий | низкая-средняя | согласованно с MIG-1 (один PR «опечатки в данных») | у4:решение 1 |

## Этап 6 — конфигурация и DX, фоном (DX)

| ID | Задача | Эфф. | Стоим. | Зависимости / примечания | Источник |
|---|---|---|---|---|---|
| DX-1 | Мёртвые зависимости: удалить `mongoose`, `@sinclair/typebox`, `@vitejs/plugin-basic-ssl`; `pino` — после проверки; **`zod` и `fastify-type-provider-zod` оставить** под VAL-1 | низкий | мин | до VAL-1 zod не трогать | у5:§8, у1:§4 |
| DX-2 | pino: `redact` (authorization, cookie, set-cookie) + уровни per-env (prod info, dev debug/pretty) | средний | низкая | — | у5:§8 |
| DX-3 | vite: задать `envDir` на корень монорепы (VITE_* заработают) либо убрать мёртвые чтения `VITE_SITE_NAME`/`VITE_API_URL` | низкий | низкая | — | у5:§4.3 |
| DX-4 | compose-хвосты: удалить мёртвые volumes, client-контейнеру не давать весь `.env`, `--frozen-lockfile` в dev-Dockerfile'ах, AOF для dev-redis | низкий | низкая | — | у5:§4.4 |
| DX-5 | Индикатор dev-режима: WARN в лог сервера после listen + заметный бейдж «DEV» на клиенте (`import.meta.env.DEV`) | средний | низкая | — | bl |
| DX-6 | Helmet: включить CSP (исключения для Swagger UI) | средний | средняя | — | bl |
| DX-7 | eslint: серверный конфиг по канону typescript-eslint (no-undef off для TS и т.п.), прогнать `--fix`, разобрать 79 проблем; решить про pre-commit hook или CI-скрипт `check` (lint+typecheck оба пакета) | средний | средняя | клиентская часть — после GT-1 | у5:§7 |
| DX-8 | Мёртвый код: `registerModules`, `checkMailDelivery.ts`, `auth.api` `/content/login`, `createUser(FormData)`, пустые `modules/users`/`stores/`, закомментированные блоки | низкий | низкая | — | у2:п.12, у4:срез 6, у5:§7 |
| DX-9 | Инвентарь `any`/`!` по списку у5:§5: `Filter<T>` в content.repository, типизация setQueryData-колбэков, `FormValues`, клиентские `!` | низкий | средняя | после GT-1/GT-2; частично уйдёт с UNI-1 | у5:§5 |
| DX-10 | Закрепить инвариант сидов «только import type» (bundle в build-seeds.mjs или комментарий + проверка) | низкий | мин | — | у5:§6 |
| DX-11 | CLAUDE.md привести к реальности (эндпоинты, vue-query, пустые модули, mailer-заглушка) + вписать новые команды `typecheck`/`lint` | средний | низкая | после GT-2, DX-7 | у1:§4 |
| DX-12 | `ExplicitPick`: один источник или зафиксированный комментом дубль | низкий | мин | — | у5:§5 |

---

## Топ-5 быстрых побед (высокий эффект / низкая стоимость)

| # | ID | Что даёт | Стоимость |
|---|----|----------|-----------|
| 1 | GT-1 | одна команда — оживает весь клиентский тулинг (lint, type-check, vitest) | мин |
| 2 | GT-2 | сервер получает ворота типов; сейчас 0 ошибок — внедрение бесплатно | мин |
| 3 | SEC-2 | перестают утекать хеши паролей и врать типы: 5 находок одним фиксом | низкая-средняя |
| 4 | SEC-3 | закрывается чтение чужих докладов + бездонный limit | низкая |
| 5 | SEC-1 | секреты перестают быть плейсхолдерами, fail-fast не даст повторить | низкая |

Следом по соотношению: SEC-5 (mailer в prod), FIX-1 (await/проглоченные
ошибки — три однострочника).

## Ключевые зависимости

- **GT-1 →** все клиентские задачи (UNI-8, UNI-9, DX-7-клиент, DX-9-клиент).
- **GT-2 →** страховка для всех серверных этапов; делать первым коммитом.
- **DX-1 ⇄ VAL-1**: zod и fastify-type-provider-zod не удалять — они
  фундамент валидации.
- **UNI-1 → UNI-4**: buildUpdate чистить после появления parseMultipart.
- **FIX-3 → UNI-2**: нормализация email до слияния registration/create.
- **FIX-6 ⇄ UNI-7**: контракт удаления и статусы успеха — один PR
  (сервер+клиент синхронно).
- ~~**SEC-4 = вариант A (решено)**; SEC-11/L3 при желании упрощается тем же
  механизмом (SET токенов юзера)~~ — примечание **снято 2026-08-10**: вариант A
  реализован *без* SET (guard читает конкретный ключ access-токена), поэтому
  упрощать L3 было нечем; L3 закрыт курсорным `SCAN`, per-user SET не заводили.
- **MIG-1/MIG-2** — в конце, отдельными PR с миграциями; UNI-6 их не трогает.

## Сводная таблица этапов

| Этап | IDs | Пунктов | Суть | Объём |
|---|---|---|---|---|
| 0. Среда и ворота | GT-1..4 | 4 | оживить тулинг, поставить typecheck-ворота | ~час |
| 1. Безопасность ✅ | SEC-1..11 | 11 | секреты, проекция user, IDOR, mailer, лимиты | закрыт 2026-08-10 |
| 2. Корректность | FIX-1..11 | 11 | await, ObjectId, roles, контракт 204, лимиты upload | дни (5/11 — FIX-1 и FIX-2 сделаны 2026-08-10, FIX-3, FIX-4 и FIX-11 — 2026-08-11) |
| 3. Валидация | VAL-1 | 1 (2 шага) | zod на вход: auth+reports → остальное | неделя+ |
| 4. Унификация | UNI-1..9 | 9 | multipart, пути, пагинация, клиентские API | неделя+ |
| 5. Миграции данных | MIG-1..2 | 2 | verificator, initialValues | дни |
| 6. Конфиг и DX | DX-1..12 | 12 | зависимости, pino, eslint, CLAUDE.md, мёртвый код | фоном |

Итого 50 пунктов (FIX-11 добавлен 2026-08-11 по ходу работы). Старт: GT-1 → GT-2 → топ-5 → этап 1 по порядку.
Развилка SEC-4 закрыта: **вариант A** (решение владельца, 2026-07-12).

**Этапы 0 и 1 закрыты полностью (2026-08-10).** GT-1..4 — сделано/неактуально;
SEC-1..9 и SEC-11 — сделано, SEC-10 — закрыт без действий (решение владельца).

**Этап 2 начат (2026-08-10):** FIX-1 сделан — в него дополнительно взят
`redis.del` без `await` в `auth.service.resetPasswordWithCode` (тот же класс,
был в источнике у4:срез 2, но в текст карточки не попал). FIX-2 сделан —
хелпер `toObjectId` разошёлся по всем 5 репозиториям, счёт мест снова оказался
больше заявленного в карточке (14 непокрытых конструкций вместо 9, плюс
переведены 13 уже существовавших ручных проверок). FIX-3 сделан 2026-08-11 —
и снова шире карточки: она называла только `users.service.create`, а
регистрозависимыми оказались ещё логин, forgot-password, reset и проверка
занятости в `registration`; в правку дополнительно вошло условие корректности,
которого в карточке не было вовсе — Redis-ключи forgot/reset обязаны строиться
из той же нормализованной строки, иначе лок из SEC-6 обходится сменой регистра.
FIX-4 сделан 2026-08-11 — единственный пока пункт, где **решение развилки
разошлось с текстом карточки**: семантику `roles: []` к серверной не приводили
и `/guard` не удаляли (разбор показал, что серверной семантики пустого массива
не существует — там обрабатывается отсутствие аргумента, а клиентское «не
пускать никого» единственное, что этот ключ вообще выражает). Развилка 3
выполнена как утверждена; сверх карточки в правку вошли фолбэк `roleRedirect`
на `admin-layout` и вывод требования авторизации из наличия `roles` — без них
явные роли замыкали кольцо редиректов, которое vue-router не обрывает.
По ходу FIX-4 нашлась и сразу закрыта дыра, которой не было ни в одном срезе:
любой пользователь мог опубликовать собственный доклад через
`PATCH /reports/update/:id` — заведена и отмечена сделанной как **FIX-11**
(туда же вошли реальные права модерации для роли manager).
Следующий — **FIX-5** (`changePassword`: runtime-проверка `type === 'refresh'`
после verify; `accessToken!` в logout → явный 401 при битом заголовке).
