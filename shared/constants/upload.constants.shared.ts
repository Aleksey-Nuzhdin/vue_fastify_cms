// Источник правды по лимитам загрузки файлов. Используется и сервером
// (регистрация @fastify/multipart в app.ts + точечные limits в контроллерах),
// и клиентом (предпроверка размера до отправки). Менять только здесь.
export const MB = 1024 * 1024

export const UPLOAD_LIMITS = {
  defaultFileSize: 10 * MB, // общий лимит: аватар и всё, что не перекрыто точечно
  storageFileSize: 100 * MB, // админский storage: files create/update
  reportFileSize: 100 * MB, // доклады: pdf с иллюстрациями, сканы
} as const

// Потолок разрешения для картинок с пользовательских путей (аватар, доклады):
// больше — отказ до обработки, а не пережатие. Админский storage не ограничен.
export const IMAGE_MAX_SIDE = 3840 // px по любой стороне (4K)
