# Backlog улучшений VFC

Пополняется по результатам фаз аудита (план — `update_plan.md`, карта — `update_1_map.md`).
Каждый пункт → отдельная задача → отдельный коммит.

> **2026-07-12**: все пункты ниже вошли в дорожную карту `update_6_roadmap.md`:
> sharp-пул → FIX-10, лимит аватара → FIX-9, dev-индикатор → DX-5, CSP → DX-6,
> `.replace('upload')` → внутри UNI-3. Работать по roadmap; сюда — только новые
> побочные находки.

## Актуальные задачи

1. **Sharp-ресайзер: лимит параллельных воркеров.**
   Уточнение по итогам фазы 1: дедупликация одинаковых задач УЖЕ есть —
   `resizeInProgress` map (`fsStorage.service.ts:60,82-83`). Осталась проблема:
   каждый ресайз спавнит новый worker thread (`fsStorage.service.ts:93`) без
   ограничения их числа — неавторизованные запросы к РАЗНЫМ картинкам/размерам
   порождают неограниченно много параллельных воркеров. Нужен пул/очередь
   (лимит 1-2 воркера) и/или ужесточение rate-limit для `/upload/images/*`.

2. **Multipart: отдельный лимит на аватар.**
   `app.ts:34-37` — глобальный лимит 100MB действует и на
   `/api/profile/upload/avatar`, доступный любому залогиненному при открытой
   регистрации. Задать маленький лимит на этом роуте (например 5MB) и
   проверить, пережимается ли аватар после загрузки.

## Бэклог

- **Индикатор dev-режима (server warning + client badge).**
  Зачем: `NODE_ENV=development` включает опасные для прода дефолты — TTL
  access-токена 30 дней (`configs/auth.config.ts:20`, см. `update_3_security.md`
  H1), а также маскирует риск слабых секретов (C1). Легко случайно запустить
  dev-конфиг там, где ожидается прод. Нужна заметная индикация.
  Что сделать:
  1. **Сервер** — при `NODE_ENV === 'development'` после старта (`server.ts`,
     после `listen`) выводить в лог явный `WARN`: работает dev-режим, TTL токенов
     раздут, секреты могут быть дефолтными.
  2. **Клиент** — при dev-сборке рисовать в шапке (`App.vue` / общий layout)
     заметную плашку-бейдж «DEV» (например, ярко-красную полосу), чтобы её нельзя
     было не заметить. Триггер — `import.meta.env.DEV` (Vite).
  Реализацию не делать в этой фазе — только зафиксировано как задача.

- **Helmet: включить CSP.** `app.ts:30-32` — `contentSecurityPolicy: false`.
  Пользовательский HTML (Quill) сейчас защищён только DOMPurify на клиенте;
  CSP — вторая линия обороны от XSS. Включить с исключениями для Swagger UI
  и инлайн-скриптов SPA, если понадобятся.

- **Семейства refresh-токенов: полный отзыв сессии.** Найдено при FIX-5
  (2026-08-13), в срезах у2-у5 этого не было. Корень: ключи в Redis плоские
  (`user:{id}:token:{type}:{uuid}`), связи между токенами одной сессии нет — по
  refresh-токену невозможно узнать, какие access-ключи выдавались в той же
  цепочке. Отсюда две несделанные вещи:
  1. `auth.service.refresh()` — закомментированный блок «подозреваем кражу»
     (реакция на переиспользование уже отозванного refresh-токена). Сейчас там
     только `throw unauthorizedError('Token revoked')`, а единственная доступная
     альтернатива — `logoutAll`, то есть выбить человека со всех устройств из-за
     одной гонки при перезагрузке страницы.
  2. `refresh()` удаляет старый refresh-ключ, а старый **access**-ключ не удаляет:
     его uuid негде взять. Ключ доживает свой TTL, и `logout` его не трогает —
     он гасит только тот uuid, что пришёл в заголовке. Значит прежний
     access-токен проходит guard ещё до 15 минут в проде и **до 30 дней** в
     dev-конфиге после выхода. Полностью чистит только `logoutAll` (смена
     пароля, reset, «выйти везде»). В проде окно узкое и требует уже украденного
     токена — терпимо, но это именно дыра, а не задумка.
  Что потребуется: id сессии (семейства) в payload обоих токенов либо per-session
  SET ключей, и отдельное решение, что делать при обнаружении переиспользования.
  Это переделка выпуска токенов, `refresh`, `logout` и формата Redis-ключей —
  задача с проектированием, а не строка в существующем методе. До неё
  закомментированный блок в `refresh()` не трогать.

- Хрупкое место (не безопасность): `upload.controller.ts:16` —
  `.replace('upload', '')` заменяет первое вхождение подстроки где угодно
  в пути; файл/папка со словом «upload» в имени станет недоступен.
